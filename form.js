const Contraseña=document.getElementById('contraseña')
const form=document.getElementById('form')

form.addEventListener('submit', e=>{
    e.preventDefault()
    let warning=''
    let 
    if(Contraseña.Value.length <6){
        warning += 'Constraseña incorrecta'
    }
})